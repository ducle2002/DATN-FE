import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const MainReportScreen = (props: Props) => {
  return (
    <View>
      <Text>MainReportScreen</Text>
    </View>
  );
};

export default MainReportScreen;

const styles = StyleSheet.create({});
