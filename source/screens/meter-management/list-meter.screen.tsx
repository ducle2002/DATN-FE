import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const ListMeterScreen = (props: Props) => {
  return (
    <View>
      <Text>ListMeterScreen</Text>
    </View>
  );
};

export default ListMeterScreen;

const styles = StyleSheet.create({});
